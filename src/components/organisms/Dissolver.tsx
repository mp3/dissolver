import * as React from 'react'
import styled from 'styled-components'

export default () => {
  return <Container>dissolver</Container>
}

const Container = styled.div`
  padding: 10px;
  text-align: center;
`
